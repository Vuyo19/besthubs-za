/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['uxwing.com', 'static.thenounproject.com', 'firebasestorage.googleapis.com']
    }
};

export default nextConfig;
